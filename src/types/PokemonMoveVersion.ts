import type DefaultDTO from "./DefaultDTO"

type PokemonMoveVersion = {
  move_learn_method: DefaultDTO,
  version_group: DefaultDTO,
  level_learned_at: number,
}

export default PokemonMoveVersion 
